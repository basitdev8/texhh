# Cloudinary upload credentials

Status: resolved

## Resolution

The replacement credentials in `.env` were verified with a real Cloudinary upload and
subsequent deletion. Product uploads are working again.

## Launch follow-up

Add real product photography through the admin panel before launch. Production must carry
the same Cloudinary environment variables as `.env`.
